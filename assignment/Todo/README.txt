The application support CRUD opration with movies stored in JSON format
{
    id : string, 
    title : string,
    director: string, 
    imdb: string
}

To run the application use : go run main.go
HTTP request can be sent through POSTMAN to verify functionality of CRUD APIs
/movies is the root directory
